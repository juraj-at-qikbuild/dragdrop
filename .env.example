# Copy to .env.local for `npm run dev`. When unset, the game is single-player only
# and the "Online" menu button stays hidden.
VITE_SERVER_URL=ws://localhost:8080

# Both optional; without them play is guest-only (no sign-in/sign-up buttons). See src/net/auth.ts.
VITE_SUPABASE_URL=https://eejvrdvzteyrwlhjfnfx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_…
