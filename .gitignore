node_modules/
# symlinked into agent worktrees: a link isn't a directory, so match the name too
node_modules
dist/
.cache/
.wrangler/
server/node_modules/
server/dist/
*.db
*.db-*
.env
*.local
# git worktrees of parallel agents
.claude/worktrees/
